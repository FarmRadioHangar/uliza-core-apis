extends layout

block content
  p Redirecting you to login widget&hellip;
  script.
    const webAuth = new auth0.WebAuth({
      clientID: '#{env.AUTH0_CLIENT_ID}',
      audience: 'https://dev.farmradio.fm/api/',
      domain: '#{env.AUTH0_DOMAIN}',
      redirectUri: '#{env.AUTH0_CALLBACK_URL}',
      responseType: 'code',
      scope: 'openid'
    });
    webAuth.authorize();

extends layout

block content
  p Redirecting you to the login widget&hellip;
  script.
    const webAuth = new auth0.WebAuth({
      clientID: '#{env.AUTH0_CLIENT_ID}',
      audience: 'http://127.0.0.1:8000/api/',
      domain: '#{env.AUTH0_DOMAIN}',
      redirectUri: '#{env.AUTH0_CALLBACK_URL}',
      responseType: 'code',
      scope: 'openid'
    });
    webAuth.authorize();
